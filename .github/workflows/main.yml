name: Send Safe Move Message

on:
  push:
    branches:
      - main  # 触发条件是推送到main分支时

jobs:
  send_message:
    runs-on: ubuntu-latest  # 使用ubuntu环境
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2  # 拉取代码

      - name: Run send message script
        run: |
          echo "发送消息功能"
          # 在这里填写你自己的发送消息的命令或者逻辑
